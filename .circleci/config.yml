version: 2.1
jobs:
  build:
    docker:
      - image: circleci/golang:1.17

    steps:
      - checkout

      - run:
          name: Build and test
          command: |
            echo "Hello World"

workflows:
  version: 2
  build:
    jobs:
      - build
